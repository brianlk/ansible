---
- hosts: localhost
  tasks:
    - block:
        - name: list something
          # execute failed tasks
          command: "ls -z"
          register: xxx
      rescue:
        - name: resuce the task
          shell: |
            touch /tmp/abcd.txt
